// Supabase server stub - will be configured later with actual Supabase credentials
// For now, we focus on the UI layer only

export const createClient = () => {
  console.warn("Supabase server client not configured yet. Using UI-only mode.")
  return null
}
